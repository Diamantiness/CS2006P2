inputValidator
==============

.. py:function:: inputValidator(n, alpha)

   Validates the modulus and multiplier for an IntricateInteger.

   :param n: (int) The modulus.
   :param alpha: (int) The multiplier.

   :returns: (bool) True if the input is valid.

   :raises ValueError: If modulus or multiplier is invalid.
